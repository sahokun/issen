/target
/.claude/
/CLAUDE.md
/CLAUDE.local.md

# prototypes/ contains standalone cargo workspaces that are not part of the
# root workspace (see prototypes/gui-plugin/README.md), so their target/
# directories aren't caught by the root /target entry above.
/prototypes/*/*/target
